import {hav,norm,clamp,isFree} from './core.js';
import {geocode,discoveryBundle,parkPlaces,roadRoute} from './sources.js';
const nearest=(p,arr,pred=()=>true)=>{let d=999;for(const x of arr){if(!pred(x))continue;d=Math.min(d,hav(p.lat,p.lng,x.lat,x.lng));}return d;};
const featureScore=(town,features,park,theme,filters)=>{let s=0;const beach=nearest(town,features,x=>x.tags?.natural==='beach'),view=nearest(town,features,x=>x.tags?.tourism==='viewpoint'),nature=nearest(town,features,x=>x.tags?.leisure==='nature_reserve'||x.tags?.boundary==='protected_area'),historic=nearest(town,features,x=>!!x.tags?.historic),surf=nearest(town,features,x=>x.tags?.sport==='surfing');const ps=park.filter(p=>hav(town.lat,town.lng,+p.latitude,+p.longitude)<18);const avg=ps.length?ps.reduce((a,p)=>a+(+p.rating||0),0)/ps.length:0,free=ps.filter(isFree).length;
if(theme==='Costa'){s+=Math.max(0,30-beach*2)+Math.max(0,12-view)+Math.max(0,12-surf);}if(theme==='Pueblos'){s+=Math.max(0,26-historic*2)+(town.place==='village'?10:0);}if(theme==='Naturaleza'){s+=Math.max(0,28-nature*1.5)+Math.max(0,10-view);}if(theme==='Mixto')s+=Math.max(0,14-beach)+Math.max(0,12-nature)+Math.max(0,10-historic);
for(const f of filters){const n=norm(f);if(n.includes('playa')||n.includes('mar'))s+=Math.max(0,15-beach);if(n.includes('surf'))s+=Math.max(0,14-surf);if(n.includes('natur'))s+=Math.max(0,14-nature);if(n.includes('mirador'))s+=Math.max(0,12-view);if(n.includes('pueblo'))s+=town.place==='village'?10:0;if(n.includes('tranquil'))s+=free?4:0;}
s+=Math.min(16,ps.length*1.5)+avg*2+Math.min(8,free*2);s+=Math.log10(Math.max(10,town.pop||10))*1.5;return{s,meta:{beach,view,nature,historic,surf,parkCount:ps.length,avgRating:avg,free}};};
const pickSpread=(rows,count,origin)=>{const out=[];for(const row of rows){if(hav(origin.lat,origin.lng,row.lat,row.lng)<12)continue;if(out.every(x=>hav(x.lat,x.lng,row.lat,row.lng)>22)){out.push(row);if(out.length>=count)break;}}return out;};
export async function planThree({originName,mandatory=[],days=7,maxDaily=250,maxTotal=1200,priority='Mixto',pace='equilibrado',detour=60,filters=[]},status=()=>{}){status('Localizando salida…');const a=await geocode(`${originName}, España`,1);if(!a[0])throw Error('No se encontró el punto de salida');const origin={name:a[0].name||originName,lat:+a[0].lat,lng:+a[0].lon};const radius=clamp(Math.max(90,maxTotal/3,detour*2),90,320);status('Buscando pueblos, costa, naturaleza y puntos reales…');const [{towns,features},park]=await Promise.all([discoveryBundle(origin,radius),parkPlaces()]);if(towns.length<4)throw Error('No hay suficientes destinos reales en el radio elegido');const target=clamp(pace==='muchas'?Math.ceil(days*.75):pace==='pocas'?Math.ceil(days*.38):Math.ceil(days*.55),2,7);const themes=priority==='Mixto'?['Costa','Pueblos','Naturaleza']:[priority,priority==='Costa'?'Naturaleza':'Costa','Mixto'];const mandatoryPts=[];for(const n of mandatory){const g=await geocode(`${n}, España`,1);if(g[0])mandatoryPts.push({name:g[0].name||n,lat:+g[0].lat,lng:+g[0].lon});}
const options=[];for(let i=0;i<3;i++){const theme=themes[i];status(`Calculando opción ${i+1}/3 · ${theme.toLowerCase()}…`);const rows=towns.map(t=>{const f=featureScore(t,features,park,theme,filters);return{...t,_score:f.s-hav(origin.lat,origin.lng,t.lat,t.lng)/18,_meta:f.meta};}).sort((x,y)=>y._score-x._score);const picked=pickSpread(rows,target,origin);const pts=[origin,...mandatoryPts,...picked].filter((x,j,arr)=>arr.findIndex(y=>norm(y.name)===norm(x.name))===j);let route;try{route=await roadRoute(pts);}catch{continue}if(route.distanceKm>maxTotal*1.25&&picked.length>2){const shorter=[origin,...mandatoryPts,...picked.slice(0,Math.max(2,picked.length-2))];route=await roadRoute(shorter);pts.splice(0,pts.length,...shorter);}const drivingPerDay=Math.round(route.distanceKm/Math.max(1,days));options.push({id:`route-option-${i}`,theme,name:`${theme} · ${days} días`,stops:pts,route,score:Math.round(pts.slice(1).reduce((s,p)=>s+(p._score||0),0)),why:`${route.distanceKm} km · ~${drivingPerDay} km/día · ${pts.length} paradas`,parkEvidence:pts.slice(1).reduce((n,p)=>n+(p._meta?.parkCount||0),0)});}
if(!options.length)throw Error('No se pudieron calcular propuestas válidas');return options;}
